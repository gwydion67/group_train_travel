import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
name : 'user' , 
initialState : {
    userInfo : []  , 
    isLoading : false , 
    error : null 

} , 




})

export default userSlice.reducer